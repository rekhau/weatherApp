# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'OpenWeatherApp' do
  use_frameworks!

pod 'WeatherConnection', :git => 'https://github.com/rekhau/WeatherConnection', :tag => '1.0.0'

  # Pods for OpenWeatherApp

  target 'OpenWeatherAppTests' do
    #inherit! :search_paths
    # Pods for testing
  end

  target 'OpenWeatherAppUITests' do
    #inherit! :search_paths
    # Pods for testing
  end

end
